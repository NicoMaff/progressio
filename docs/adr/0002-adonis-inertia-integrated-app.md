# AdonisJS And Inertia Integrated App

Progressio uses AdonisJS with InertiaJS as an integrated local web application instead of separating a backend API from an independent SPA. This matches the local SQLite work-file model: AdonisJS owns routing, application actions, and persistence, while Inertia keeps the frontend rich without introducing a premature public API boundary.
