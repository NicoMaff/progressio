# Local SQLite Work File

Progressio uses a local SQLite work file as the unit of work instead of authentication, user accounts, or a server database. One work file represents one school year for one subject, which keeps the application portable and simple for a single teacher while leaving multi-year comparison and multi-subject workflows to explicit import or comparison features.
