# React TypeScript Storybook And UnoCSS

Progressio uses React and TypeScript for its Inertia frontend and reusable UI package, with Storybook for component documentation and visual iteration. Styling uses a root UnoCSS configuration with the Wind 4 preset shared across the workspace; the UI package focuses on framework-independent component behavior and consumes the shared styling conventions instead of owning the theme.
